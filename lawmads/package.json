{
  "name": "lawmads",
  "version": "1.0.0",
  "private": true,
  "description": "Lawmads — The Legal Technology Academy platform (Law Tech Labs)",
  "license": "UNLICENSED",
  "type": "module",
  "workspaces": ["packages/shared", "apps/api", "apps/web"],
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "build": "npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web",
    "preview": "vite preview --config apps/web/vite.config.ts apps/web",
    "typecheck": "npm run typecheck -w packages/shared && npm run typecheck -w apps/api && npm run typecheck -w apps/web",
    "lint": "eslint .",
    "test": "npm run test -w packages/shared && npm run test -w apps/api",
    "test:e2e": "playwright test -c tests/e2e/playwright.config.ts",
    "db:migrate": "tsx apps/api/src/db/migrate.ts",
    "db:seed": "tsx apps/api/src/db/seed/index.ts",
    "db:reset": "tsx apps/api/src/db/migrate.ts --reset && tsx apps/api/src/db/seed/index.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.0",
    "concurrently": "^9.0.1",
    "eslint": "^9.12.0",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.8.0"
  }
}
